# CALENDAR UI SETUP INSTRUCTIONS

## 📦 Installation

Before using the calendar components, you need to install the required package:

```bash
cd talkplatform-frontend
npm install react-big-calendar
npm install @types/react-big-calendar -D
```

## ✅ After Installation

The calendar components will work automatically. No additional configuration needed.

## 📍 Available Pages

### For Students:
- `/teachers/[id]/book` - List view (existing)
- `/teachers/[id]/book-calendar` - **NEW** Calendar view

### For Teachers:
- `/teacher/availability` - List view (existing)
- `/teacher/availability-calendar` - **NEW** Calendar view

## 🎯 Features

- ✅ Full calendar view with month/week/day/agenda
- ✅ Timezone handling (UTC → Local Time)
- ✅ Click to select/book slots
- ✅ Visual indicators
- ✅ Responsive design

---

**Note:** Calendar will not work until `react-big-calendar` is installed.

