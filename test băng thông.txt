⛬  ✅ Created Bandwidth Monitor System!

   Tôi đã tạo system để monitor bandwidth, nhưng cần integrate vào WebRTC. Đây là 3 cách để monitor bandwidth trong meeting:

   **Cách 1: Console.log Built-in (Đơn giản nhất - TEST NGAY)**

   Mở Chrome DevTools trong meeting room:

   javascript
     // 1. Mở Console (F12)
     // 2. Paste code này:

     setInterval(async () => {
       const pc = window.pc || document.querySelector('video').srcObject?.getPeerConnections?.()?.[0];

       if (!pc) {
         console.log('⚠️ No peer connection found');
         return;
       }

       const stats = await pc.getStats();
       let inbound = 0, outbound = 0;

       stats.forEach(report => {
         if (report.type === 'inbound-rtp' && report.bytesReceived) {
           inbound += report.bytesReceived;
         }
         if (report.type === 'outbound-rtp' && report.bytesSent) {
           outbound += report.bytesSent;
         }
       });

       console.log('📊 Bandwidth:', {
         download: `${(inbound / 1024 / 1024).toFixed(2)} MB`,
         upload: `${(outbound / 1024 / 1024).toFixed(2)} MB`
       });
     }, 2000);

   **Cách 2: Chrome WebRTC Internals (Chi tiết nhất)**

   1. Mở meeting room
   2. Mở tab mới, truy cập: chrome://webrtc-internals/
   3. Sẽ thấy:
     •  ✅ Real-time graphs cho bandwidth
     •  ✅ Packet loss, jitter, RTT
     •  ✅ Video/Audio bitrate
     •  ✅ All connection stats

   **Cách 3: Thêm UI Monitor (Đã tạo sẵn components)**

   Để thêm visual monitor vào UI, cần modify useWebRTC để export peerConnections. Tôi có thể làm điều này nếu bạn muốn!

   🎯 Test Bandwidth Ngay:

   Option A - Chrome WebRTC Internals (Recommended):
   1. Join meeting ở /meetings/:id
   2. Mở tab mới: chrome://webrtc-internals/
   3. Xem real-time graphs!

   Option B - Console Monitor:
   1. Join meeting
   2. F12 → Console
   3. Paste code trên
   4. Xem logs mỗi 2 giây

   Bạn muốn tôi thêm visual UI monitor vào meeting room không? Cần modify useWebRTC hook một chút. 📊
